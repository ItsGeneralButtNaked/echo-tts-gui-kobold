# ── Core web framework ────────────────────────────────────────────────────────
flask>=3.0,<4.0
requests>=2.31,<3.0

# ── LLM / STT / TTS ───────────────────────────────────────────────────────────
faster-whisper>=1.0,<2.0          # Whisper transcription (web STT endpoint)

# ── SSL cert generation ───────────────────────────────────────────────────────
cryptography>=42.0,<45.0          # self-signed cert for HTTPS (ssl_cert.py)

# ── RAG — semantic tier (optional but recommended) ────────────────────────────
# Remove these two lines if you only need keyword-based RAG.
numpy>=1.26,<3.0
sentence-transformers>=3.0,<4.0   # all-MiniLM-L6-v2 embeddings
faiss-cpu>=1.8,<2.0               # vector index (use faiss-gpu if you have CUDA)

# ── Desktop STT / VAD (optional — only needed for the desktop app) ────────────
# Uncomment if running ecko_desktop.py:
# sounddevice>=0.4,<0.6
# torch>=2.0                        # required for Silero VAD (falls back to energy VAD without it)
