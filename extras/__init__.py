"""
extras — standalone fun/visual modules for Ecko.

These are self-contained helpers that don't belong in core (no LLM coupling)
and aren't web routes. Primarily server-side support for UI visual features.

Current modules:
  ascii_art   — ASCII art detection and server-side helpers
  python_code — Python / code-block detection, extraction, and TTS stripping
"""
